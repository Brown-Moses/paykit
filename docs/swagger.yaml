basePath: /
host: localhost:8080
info:
  contact: {}
  description: Production-ready multi-tenant payment notification engine for MTN MoMo.
  title: PayKit — Mobile Money Webhook Engine
  version: "1.0"
paths:
  /health:
    get:
      description: Returns server and database status. Use this to verify the service
        is running before sending webhooks.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              database:
                type: string
              status:
                type: string
            type: object
        "503":
          description: Service Unavailable
          schema:
            properties:
              database:
                type: string
              status:
                type: string
            type: object
      summary: Health check
      tags:
      - System
  /metrics:
    get:
      description: Returns transaction volume and delivery success rate.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              delivery_success_rate:
                type: string
              transactions_failed:
                type: integer
              transactions_successful:
                type: integer
              transactions_total:
                type: integer
              uptime:
                type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            properties:
              error:
                type: string
            type: object
      summary: System metrics
      tags:
      - System
  /transactions:
    get:
      description: Returns paginated transactions for the authenticated merchant.
        Filter by status or look up a specific order using external_id.
      parameters:
      - description: Look up by order ID (e.g. order_987)
        in: query
        name: external_id
        type: string
      - description: 'Filter by status: SUCCESSFUL, FAILED, PENDING'
        in: query
        name: status
        type: string
      - description: 'Page number (default: 1)'
        in: query
        name: page
        type: integer
      - description: 'Results per page (default: 20)'
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              count:
                type: integer
              data:
                type: array
              limit:
                type: integer
              page:
                type: integer
            type: object
        "401":
          description: Unauthorized
          schema:
            properties:
              error:
                type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            properties:
              error:
                type: string
            type: object
      security:
      - BearerAuth: []
      summary: List and filter transactions
      tags:
      - Transactions
  /transactions/{id}:
    get:
      description: Returns a single transaction scoped to the authenticated merchant.
        The paid field gives a simple true/false answer.
      parameters:
      - description: Provider transaction ID (e.g. TX-001)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              amount:
                type: string
              currency:
                type: string
              external_id:
                type: string
              paid:
                type: boolean
              provider_tx_id:
                type: string
              received_at:
                type: string
              status:
                type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            properties:
              error:
                type: string
            type: object
        "404":
          description: Not Found
          schema:
            properties:
              error:
                type: string
            type: object
      security:
      - BearerAuth: []
      summary: Get transaction by provider ID
      tags:
      - Transactions
  /transactions/{id}/deliveries:
    get:
      description: Returns all webhook delivery attempts made to the merchant's webhook
        URL for a given transaction. Shows attempt number, status, HTTP response code,
        and any error messages.
      parameters:
      - description: Provider transaction ID (e.g. TX-001)
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            properties:
              attempts:
                type: integer
              logs:
                type: array
              transaction_id:
                type: string
              webhook_url:
                type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            properties:
              error:
                type: string
            type: object
        "404":
          description: Not Found
          schema:
            properties:
              error:
                type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            properties:
              error:
                type: string
            type: object
      security:
      - BearerAuth: []
      summary: Get delivery attempts for a transaction
      tags:
      - Delivery Logs
securityDefinitions:
  BearerAuth:
    description: 'Enter your API key as: Bearer pk_live_xxx'
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
