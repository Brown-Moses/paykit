DATABASE_URL=postgres://user:password@localhost:5433/paykit?sslmode=disable
MOMO_WEBHOOK_SECRET=your_secret_from_mtn_portal
PORT=8080

# Optional — restrict webhook to MTN IP ranges only
# Leave empty to allow all IPs (default)
# MTN MoMo IP ranges: check your MTN developer portal
ALLOWED_IPS=